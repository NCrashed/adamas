-- Рекурсия. `let rec` -- сахар: разворачивается в `fix` при опускании в core.
-- Внутри собственной правой части `fact` мономорфна, обобщается уже после.

let rec fact = \n ->
  if n < 1 then 1 else n * fact (n - 1)
in
fact 10
