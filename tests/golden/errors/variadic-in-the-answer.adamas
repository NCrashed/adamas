-- `...` в позиции ответа (§5.3).
--
-- Вариадическая часть есть хвост списка **аргументов**, и отвечать ею нечем.
-- Отказ здесь свой, а не общий «имя не найдено»: написанное автором - законная
-- отметка в незаконном месте, и сказать надо про место.

data Unit where
  MkUnit : Unit

effect Foreign

extern "C" fn snprintf : Array n UInt8 -> UInt64 -> ...

main : UInt64
main = 1
