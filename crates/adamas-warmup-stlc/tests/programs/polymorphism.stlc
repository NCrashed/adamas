-- Обобщение в `let`: `id` получает схему и инстанцируется в двух разных
-- типах. С параметром лямбды это не сработало бы -- он мономорфен.

let id = \x -> x in
let pick = \b -> if id b then id 1 else id 0 in
pick true
