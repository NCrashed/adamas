-- Отрицательный литерал беззнаковому типу не отдаётся (§4.3).
--
-- Три формы литерала - три класса преобразования, и `-1` идёт по `FromInt`.
-- Ближайший проходящий сосед - `eval/primitives.adamas`, где тот же литерал
-- стоит под `Int64` и считается.

low : UInt8
low = -1
