-- ABI, которого уровень 1 не знает (§5.3).
--
-- §5.3 называет основой **прямой C ABI**, и второго уровень 1 не обещает.
-- Написанное показано дословно, вместе с кавычками: спутать `"C"` с `"c"`
-- легко, и отказ обязан показать прочитанное, а не своё представление о нём.

data Unit where
  MkUnit : Unit

effect Foreign

extern "stdcall" fn adamas_probe_mix : UInt64 -> UInt64

main : UInt64
main = 1
