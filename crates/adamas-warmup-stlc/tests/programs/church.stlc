-- Числа Чёрча. Программа возвращает функцию, поэтому в снапшоте видно
-- полиморфный тип, а не конкретное значение.

let zero = \f -> \x -> x in
let succ = \n -> \f -> \x -> f (n f x) in
let three = succ (succ (succ zero)) in
three
