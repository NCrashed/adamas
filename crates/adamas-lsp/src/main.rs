//! LSP-сервер Adamas. Заглушка: реализация — Фаза 3 (§7.2, §9).

fn main() -> std::process::ExitCode {
    eprintln!(
        "adamas-lsp: заглушка. Language server появится в Фазе 3 (см. adamas-design.md §7.2, §9)."
    );
    std::process::ExitCode::FAILURE
}
