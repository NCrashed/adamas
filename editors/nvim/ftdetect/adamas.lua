-- Расширение `.adamas` - тип файла `adamas`.
--
-- `vim.filetype.add`, а не автокоманда `BufRead`: так тип ставится и буферу без
-- файла на диске, а сервер имени файла всё равно не знает - текст он получает
-- уведомлением.
vim.filetype.add({ extension = { adamas = 'adamas' } })
