# Демо Фазы 9: астероиды на SDL2 (§9, demo target).
#
# `[link]` нужна дважды и по-разному: компоновщику - чтобы связать, машине -
# чтобы найти символ `dlopen`'ом. Список поэтому один.
[package]
name = "asteroids"
root = "."
entry = "Main"

[link]
libraries = ["SDL2"]
