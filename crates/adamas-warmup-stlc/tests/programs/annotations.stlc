-- Аннотация переключает вывод в режим проверки. Тип параметра `f` берётся
-- из неё напрямую, метапеременная не создаётся -- ради этого bidirectional
-- и выбран.

let apply = (\f -> \x -> f x : (Int -> Bool) -> Int -> Bool) in
apply (\n -> n < 10) 3
