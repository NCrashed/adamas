-- Литерал не помещается в ширину типа (§4.11).
--
-- `Int8` знаковый, и половина его - до 127; `128` записалось бы как `-128`,
-- то есть молча не тем числом. Ближайший проходящий сосед - `128 : UInt8`,
-- строкой ниже; отвергается только вторая.

fits : UInt8
fits = 128

over : Int8
over = 128
