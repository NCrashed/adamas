# Волна 4 Фазы 6: вторая форма понижения

Продолжение `docs/phase6-plan.md`. Волны 0–3 пройдены, фаза закрыта записью
2026-09-11 с невзятым milestone и одним долгом — второй формой понижения.
Этот документ разбивает долг на треки.

## Положение

**Долг принадлежит Фазе 6, а не Фазе 7.** Итоговые записи 2026-09-11 отдали
вторую форму «Фазе 7» — это поправляется здесь: §9 резервирует Фазу 7 за
LLVM-эмиттером, SIMD и native concurrency, то есть за тем, что идёт **после**
C-бэкенда. Эффекты в C — предмет Фазы 6 («Codegen через C + Perceus»), и её
milestone без них не берётся. Волна 4 — доводка той же фазы.

**Решено и готово всё, кроме самого понижения.** Две записи ABI 2026-09-08
отвечают на вопросы формы: кадры селективно по трёхзначному вердикту §3.4;
вектор evidence скрытым первым аргументом, ручка стека вторым; сегмент
продолжения — цепочка кадров-объектов с метками машины; канонические сигнатуры
`adamas_lowered_first` / `adamas_lowered_second`; отчуждённый кадр — код формы
`adamas_frame_code`, среду заимствует, ответ идёт следующему кадру; стек крутит
`adamas_kont_run`. Рантайм написан и покрыт тестами: `frame.c`, `evidence.c`,
трёхзначный `adamas_evidence_lookup`, `adamas_kont_abort`, `adamas_kont_cut`,
`adamas_segment_unwind`, `adamas_resumption_drop`, метки `HANDLER … CLOSING`.
Машина интерпретатора — образец семантики, отревьюированный и линейный по
времени; понижению предписано **повторить её, а не изобрести**. Недостаёт
одного: понижения, которое кадры ставит.

**Мера на старте.** `TAKEN` — 32, взято 37 программ корпуса из 79; отказов 42,
из них 39 — эффекты и питомник. Состав хвоста: ~40 фикстур с хендлерами, из
них ~11 держатся на файберах и питомнике. Питомник волне **не принадлежит**:
`adamas.h` говорит прямо — метка `NURSERY` не обслуживается ничем, таблица
файберов заводится вместе с ними, Фаза 7. Значит потолок волны — эффектный
хвост без питомника, и честная цель по корпусу: `TAKEN` с 32 до ~60; точный
список снимается прогоном на старте трека A и записывается сюда числом.

**Список снят прогоном 2026-09-11, до трека A, и он точнее оценки.** Взято 37
из 79, отвергнуто 42. (Корпус с тех пор вырос: трек C добавил в него две
фикстуры, и знаменатель стал 81.) Из них **эффектных 39** (`#handle.*` — 37, `#handleMulti`
— 2 в этом счёте вместе с `#handleState`), плюс `emit` у `prelude`. Файберных
**десять**, и вот они поимённо: `cancel`, `cancel-bystander`, `nursery`,
`nursery-abort`, `spawn-local`, `task`, `task-handoff`, `task-typed`, `fibers`,
`await-value` (последняя падает не на хендлере, а на постулате `withNursery`).
Значит **потолок волны — 69 из 79**, а не ~60: непитомничного хвоста 32
программы, и оценка была занижена на девять.

**`region-strategy-in-io` держится не на эффектах**, вопреки ожиданию
milestone 1: её отказ — «ответ недобранного вызова», единственный такого рода
в корпусе. Значит трек B получит её не даром: сперва надо снять этот отказ, и
он к хендлерам отношения может не иметь вовсе. Мерить его — работа B, но знать
о нём надо до входа.

## Milestone волны

Три меры, ни одна не выводится из остальных.

1. **Корпус:** эффектный хвост без питомника переезжает в `TAKEN` со сверкой
   `agreed` — каждый прогон отвечает тем же, чем машина, и кончает с нулём
   живых блоков. `region-strategy-in-io` доезжает до C, и пользовательская
   стратегия различима числами наравне со встроенными (вопрос 161 дорешён
   делом).

   *Поправка трека B.* Первая половина пункта — про `region-strategy-in-io` —
   названа неверно и волной не берётся: фикстура держится на боксировании
   плоского значения (§5.1) и на запечатанном модуле, чей член не
   специализируется, а не на эффектах. Второе записано в шапке
   `eval/region-strategies` до волны, включая вывод «понижением тогда не
   берётся». Вторая половина — заказчик пункта — выполнена другим свидетелем;
   см. раздел трека B.
2. **Питомник остаётся отвергнутым по имени.** Сегодня один отказ «`#handle.L`
   — эффект» накрывает всё; после волны файберная программа обязана падать
   собственным отказом (`#fiber.*` — питомник, Фаза 7), иначе остаток
   замаскируется под недоделку волны.
3. **Первый бенчмарк понижения.** Чистый сосед против того же алгоритма под
   tail-resumptive хендлером; число фиксируется в §13 как точка отсчёта для
   вопроса 74 — до инлайнинга операция платит за evidence-косвенность, и эту
   цену надо знать, а не оценивать. §6 перестаёт быть непроверенным целиком.

## Волна решений

Коротка, потому что ABI решил почти всё. Каждый пункт — решение с ценой,
принимается до трека, который на нём стоит; цена варианта, названная словами,
— заявка на прогон.

1. **Форма функции — по row написанного типа.** Пустая row — первая форма,
   непустая — вторая. Тоньше не надо: «способна оказаться под общим
   хендлером» статически не отличима от «под любым», пока хендлер не виден в
   точке (вопрос 74), а инлайнинг — оптимизация после волны. Цена: функция под
   заведомо tail-resumptive меткой платит за кадр — её снимет вопрос 74, и
   бенчмарк milestone'а ровно эту цену меряет.
2. **Вердикт ветки — синтаксисом на месте `handle`.** Tail-resumptive (ветка —
   `resume` в хвосте), абортивная (`resume` не зовётся), общая. Считается при
   понижении `#handle.L`, кладётся в запись evidence: tail-ветка зовётся
   операцией на месте, без снятия сегмента. Это не анализ по графу — свойство
   написанной ветки; межпроцедурного вердикта волна не заводит.
3. **Единица дробления тела второй формы — точка приостановки.** Кадр ставится
   на вызов второй формы и на операцию; максимальный чистый отрезок между
   ними бежит обычным C. Мельче — плата трамплину на каждом шаге; крупнее —
   не приостановить. `adamas_kont_run` уже трамплин, дробление — работа
   понижения (дефункционализация продолжений в `adamas_frame_code`).
4. **Соответствие кадров: машина ↔ рантайм — таблицей.** `Branch` ↔ кадр
   ветки, `Settling` ↔ кадр применения ответа к состоянию (`#handleState`),
   `Closing` ↔ `MARK_CLOSING`, `Masking`/`Suppressing` — носимые (считает
   вектор), `Nursery` — носимая и не обслуживаемая. Таблица живёт в шапке
   модуля понижения; сверка — `agreement` плюс прицельные свидетели порядка
   деструкторов. Второй записи модели раскрутки не заводится: её держит
   рантайм, понижение ставит кадры.
5. **Ветки хендлера — статические функции со средой кадра.** Замыкание общего
   вида не нужно: формы веток известны месту `handle`, и трамплин им не
   нужен.
6. **Вопрос 12 (IO на верхнем уровне) волной не решается.** Корпус гасит `IO`
   внутри (`runIO`), `main` чист; недогашенная метка на верхнем уровне —
   отдельное решение, и волна его не предрешает.

## Треки

Цепочка, параллелить почти нечего — каждый читает предыдущий. Критерий всюду —
программа: пара фикстур либо прогон со сверкой `agreed`, мутант обязателен.

**A. Селекция формы и скрытые аргументы — сделано 2026-09-11.** `lower`
помечает определения формой по row типа (решение 1), эмиттер даёт вторым
сигнатуру `adamas_lowered_second`, вызовы прокладывают вектор и ручку;
`NULL`-пути `adamas_apply` становятся настоящей передачей. Хендлеров ещё нет —
эффектная программа по-прежнему отвергается, но уже на `#handle.L`, а не на
форме функции. Критерии: ни одна программа `TAKEN` не меняет ни стороны, ни
числа блоков, ни текста первой формы (регресс — прогоном до и после); функция
с непустой row получает два скрытых аргумента, и это видно текстом
порождённого C.

*Как вышло.* Прогон до и после дал одно и то же: 37 из 79, те же имена, те же
числа блоков, **побайтово тот же C** у всех 37; изменились только тексты
отказов. Свидетели — `crates/adamas-codegen/tests/forms.rs`.

- **Пустая row — это «без меток», а не «без хвоста».** Auto-lift (§3.4)
  дописывает свежую row-переменную всякой позиции сигнатуры, поэтому у чистой
  `sum : List Int -> Int` тип есть `List Int -> {| e0} Int`. Считай хвост
  меткой — и вторую форму получила бы каждая функция программы; измерено
  мутантом.
- **Асимметрия скрытых аргументов удвоена.** Порядок записан в одном месте
  (`HIDDEN`/`FORWARD` в `emit_c`), а сборка порождённого C идёт с
  `-Werror=incompatible-pointer-types`: перестановка вектора и ручки теперь не
  собирается вовсе, потому что типы у них разные. Ловится дважды — текстом
  сигнатуры и компилятором.
- **Первая форма второй прямым вызовом не зовёт**, и это проверяется, а не
  предполагается (`EmitError::Hidden`): взяться такая пара из исходника не
  может — погашение row не пропустит, — а `NULL` на месте вектора собрался бы
  и упал в рантайме.
- **Развилка, оставленная открытой: форма лямбды.** Row решает форму по
  **написанному** типу, а у связывания лямбды его в ядре нет вовсе — там же,
  где нет и её представления. Сегодня лямбда первой формы, и это беспредметно:
  операцию она произвести не может. Треку B она придёт вычислением под
  `handle` и обязана стать второй; row для неё живёт в ожидаемом типе позиции,
  а типов понижение по выражениям не носит — это его работа, названная здесь
  заранее.

**B. Хендлер и операции без снятия сегмента — сделано 2026-09-11.** Понижение
`#handle.L`: кадр `HANDLER`, новый вектор копией родителя плюс запись, ветки
статическими функциями (решение 5), `return`-ветка на нормальном выходе.
Операция: `adamas_evidence_lookup`, три ответа как в `adamas.h` — `HANDLER` и
tail-resumptive ветка на месте (решение 2), `SUPPRESSED` — `adamas_kont_abort`
и немедленный возврат, `MISSING` — ошибка. Общая ветка на этом треке ещё
отвергается — по имени, со ссылкой на трек D. Критерии: `runIO`-семейство
корпуса в `TAKEN`; `region-strategy-in-io` в C, стратегия различима числами
блоков и адресами, как встроенные в `tests/region.rs`; маска — фикстурой с
двумя одноимёнными хендлерами (`skip` у `adamas_evidence_lookup` наблюдаем
ответом).

*Как вышло.* Взято **46 из 79** против 37; `TAKEN` с 32 до 41. Девять новых —
эффектные: `alias-computation`, `effect-multiplicity`, `label-names-its-binder`,
`module-effect`, `nested-rowed-signature`, `region-allocates-and-reads`,
`region-bound-in-the-argument`, `signature-effect`,
`signature-effect-parameterized`. Каждая сверена с машиной и кончает нулём
живых блоков. Свидетели порядка и выбора — `crates/adamas-codegen/tests/handlers.rs`,
восемь мутантов поимённо в шапке коммита.

- **Рантайму недоставало не понижения, а связи.** Операция приходит к хендлеру
  через вектор evidence, то есть с кадром на руках и без единого статического
  знания о том, чьи это ветки; у кода кадра нет ни ручки стека, ни номера
  операции, а замыкание в среде стоило бы ячейки кучи на ветку. Заведены
  `adamas_handler_code` полем кадра, `adamas_kont_handler`,
  `adamas_frame_perform` и `adamas_kont_leave` — последний потому, что
  нормальный выход в направленном стиле снимает кадр сам, пока тела не
  дроблены (трек D). Среда кадра уехала с 48-го байта на 56-й.
- **Форма лямбды — вторая, и цена выбора ноль.** Развилка, оставленная треком
  A, закрыта не угадыванием row: прямого вызова у лямбды не бывает вовсе,
  зовут её через `adamas_apply`, а граница замыкания несёт оба скрытых
  аргумента при любой форме. Первая форма поэтому не теряет ни одного прямого
  вызова, а лямбда под `handle` получает возможность произвести операцию.
- **Чистая функция с хендлером — корень своего стека.** `runIO` гасит `IO`
  внутри себя, наружу не отдаёт и по решению 1 остаётся первой формой, а кадр
  ставить надо. Законно это ровно потому, что row её пуста: операции наружу не
  уходит ни одной (§3.4), и всё, что под ней производится, гасится внутри неё
  же. «Стек один» (§10 вопрос 144) этим не нарушается — он один на всё, что
  друг друга видит. Отсюда же названное исключение к правилу трека A: под
  `handle` первая форма зовёт вторую прямым вызовом законно, и `EmitError::Hidden`
  об этом знает.
- **Вердикт ветки считается по нормализованному телу.** Решение имплисита
  приезжает бета-редексом по всему контексту и называет каждое связывание,
  включая резумпцию; считай по написанному — и всякая ветка с имплиситом в
  аргументе оказалась бы «зовущей резумпцию и в хвосте, и до него». Измерено
  на `region-allocates-and-reads`.
- **`region-strategy-in-io` не доехала, и держится это не на эффектах** —
  как и было сказано во «Положении», но причин оказалось две, и обе названы
  корпусом до волны. Первая: `body : … -> {IO} Int64` уходит значением в
  полиморфный `runIO`, то есть плоское значение через границу замыкания, —
  боксирование §5.1, названная граница среза. Вторая: `Arena` запечатана
  (`:>`), а запечатанный член специализации не получает, поэтому обобщённый по
  нагрузке `store` получает плоский `Int64` в указательный параметр — это
  дословно записано в шапке `eval/region-strategies`, включая вывод
  «понижением тогда не берётся». Тем же вторым блокируется
  `region-strategy-handled`.
- **Заказчик milestone 1 всё-таки доехал.** «Пользовательская стратегия
  различима числами наравне со встроенными» показана не корпусной фикстурой, а
  `tests/strategy.rs`: одна и та же проба под двумя хендлерами (`keep -> resume
  True` против `False`) даёт разные хендлы, собирается, считает как машина и
  кончает нулём живых блоков. Вопрос 161 дорешён делом; фикстура, на которую
  ссылался milestone, к делу отношения не имела.
- **Первый бенчмарк понижения (milestone 3) не мерится, и по той же причине.**
  Шов `benches/native.rs` готов, хендлер поставлен, а множитель у пробы
  `Int64` — плоский. Отказ теперь называет **это**, а не хендлер; появится
  боксирование — точка `native/handled` замерится без правки файла.
- **Маска отвергается по имени.** Число пропусков (`skip`) вектору не считает
  никто: `#mask.L` кадром носится, а счёт §3.4 отдаёт вектору, и откуда его
  берёт место операции — не решено ничем. Критерий маски выполнен другим
  свидетелем, тем самым, который план и назвал: фикстура с двумя одноимёнными
  хендлерами, где ответ различает внутренний от внешнего.
- **Питомник получил собственный отказ** (milestone 2), но пока только у
  `await-value`: у остальных девяти файберных программ раньше него встаёт
  общая ветка, то есть трек D. Дойдут они до питомника вместе с ним.
- **`sealed-effect` осталась за бортом по чужой причине.** `pending : Counted`
  с `type Counted = {Tick} Nat`: арность берётся у типа (§10 вопрос 153), а
  синоним она не разворачивает, поэтому эта-достройка не срабатывает и
  операция оказывается «значением». Правка — в счёте арности, не в хендлерах.

**Что досталось трекам C–E от B.** Кадр `HANDLER` ставится и снимается,
вердикт ветки считается, метки пронумерованы (`Program::labels`), площадки
`handle` лежат таблицей (`Program::handlers`), ветки эмитятся статическими
функциями со средой кадра. Трекам C и D остаётся заменить ответ ветки: вместо
«ответ ветки и есть значение операции» — снятие сегмента (`adamas_kont_cut` /
`adamas_segment_unwind`) на месте, где сегодня стоит вызов ветки, то есть в
`Emitter::performing`. Кадров под приостановку понижение не ставит ни одного —
это и есть дробление тел, сердце трека D.

**C. Абортивная ветка и исключительные exit-points — сделано 2026-09-11.**
Ветка без `resume`: снятие сегмента `adamas_segment_unwind`, деструкторы LIFO
по дороге, ответ — из ветки. Кадры `MARK_CLOSING` ставит понижение по
`#closing` ядра — узел выхода из scope, который волна 3 оставила «имя стоит
по-прежнему». Пункт 6 волны 3 этим дорешивается: очистка ресурса на
исключительных путях. Критерии: `Except`-подобные фикстуры корпуса в `TAKEN`;
порядок деструкторов — ценой и текстом, как в пробах пункта 6; обрыв
деструктора — свидетель на `adamas_kont_abort` (кадры оборванного закрыты,
раскрутка ниже продолжается — семантика `SUPPRESSED` из `adamas.h`, сверенная
с машиной по `eval/interpreter.adamas`).

*Как вышло.* Взято **48 из 81** против 46 из 79; `TAKEN` с 41 до 43. Прибавка —
две новые фикстуры, `eval/abortive-except` и `eval/abortive-cleanup`:
корпусной `Except`-подобной программы, которую держала бы **только** абортивная
ветка, в нём не было ни одной — `interpreter` падала на `fail` первой, но за
ней у неё стоят `#handleState`, общая ветка и маска, и она осталась треку D.
Свидетели — `crates/adamas-codegen/tests/abortive.rs`, девять штук; мутантов
четырнадцать, одиннадцать пойманы, трое выжили с доводом.

- **C-кадры между операцией и хендлером — цена, которой в плане не было.**
  Ветка режет сегмент и раскручивает его, это модель, и она вся в рантайме. Но
  кадры C, стоящие между операцией и её хендлером, снимает **возврат**: пока
  тела не дроблены (трек D), другого пути из них нет. Ручка стека поэтому
  носит три поля — `aborting`, `target`, `answer`, — порождённый код читает их
  после всякого вызова, способного произвести, а место `handle` сверяет
  `target` со своим кадром и узнаёт, ему ли ответ. Уйдёт это вместе с
  дроблением: у дроблёного кода таких кадров нет.
- **Уборку на этом пути печатает эмиттер, и узла в IR у неё быть не может.**
  Возврат по обрыву обязан отдать всё, что кадр держит, иначе Perceus
  перестаёт быть без течи на исключительном пути. Считается это тем же счётом,
  каким владение ведёт `perceus`; четвёртое место в `tests/seam.rs` названо
  поимённо. Изнутри вычисления под `handle` уход идёт **на метку** за ним, а не
  возвратом: ответ обрыва ловит этот самый хендлер. Уборка там частичная — ровно
  то, что отдал бы пропущенный кусок, — и считается разностью «сколько
  вычисление потребляет целиком» и «сколько потреблено до обрыва».
- **Деструктор второй формы — замыкание, и трамплин у него забирающий.**
  Раскрутку ведёт рантайм, позвать он умеет только значение; названная цена —
  ячейка кучи на scope, которой первая форма по-прежнему не платит. Трамплин
  при этом **не дублирует** слоты, в отличие от общего: кадр `CLOSING` —
  единственный владелец замыкания, зовут его раз. Дублируй — и ресурс достался
  бы деструктору разделённым, FBIP внутри него не сработал бы никогда, а
  порядок деструкторов перестал бы наблюдаться ценой. Измерено мутантом: с
  дублированием обе подстановки LIFO-пробы дают одно число.
- **Ответ деструктора отбрасывает он сам.** Раскрутка о его типе не знает и
  дропает мелко, без детей; деструктор, отвечающий боксом, тёк. Связывание и
  дроп ставит понижение, обычным правилом `perceus` — тем же, каким это сделано
  у первой формы.
- **Путь `SUPPRESSED` прибирал только аргументы операции.** Деструктор,
  оборванный посреди работы, уносил с собой всё остальное. Найдено фикстурой
  `abortive-cleanup`, где `closeLoud` производит `fail` к подавленной метке.
- **Три мутанта нашли отсутствующих свидетелей**, и это половина ценности
  прогона: уборка временного (ячейка, посчитанная до операции); нормальный
  выход второй формы (все прежние пробы обрывались, и кадр `MARK_CLOSING` на
  нормальном пути не снимал никто — без `adamas_kont_close` прогон падает);
  пол у раскрутки (под абортивным хендлером не стояло живого).
- **Порядок «ветка, потом деструкторы» свидетеля не имеет, и это измерено.**
  Мутант, раскручивающий сегмент до ветки, выжил: ответ деструктора
  отбрасывается, а кадр хендлера жив до конца раскрутки и держит среду ветки,
  поэтому уникальным общее значение не достаётся ни ветке, ни деструктору.
  Порядок оставлен машинным; различающая программа придёт с живой резумпцией.
- **`adamas_kont_run_to` — единственная добавка к модели**, и это тот же цикл с
  полом: раскрутка обязана кончиться там, где стоял хендлер, потому что ниже
  стоит живой C-кадр, который её запустил. `adamas_kont_run` стал его частным
  случаем; второй копии модели не завелось.

**Что досталось трекам D–E от C.** Кадр `MARK_CLOSING` ставится и снимается,
`Expr::Closing` несёт код деструктора и его среду полями — как площадка
хендлера. Вердикт ветки двузначен на входе (`Branch::abortive`), третий
по-прежнему отвергается по имени. Обрыв в полёте держит ручка стека; дробление
тел его снимет — у дроблёного кода C-кадров между операцией и хендлером нет, и
`adamas_kont_run` возвращается сам. Уборка ухода (`Emitter::escape`,
`Emitter::abandoned`) уйдёт вместе с ним.

**D. Одношот общего вида — сделано 2026-09-12.** *Поправка трека C.* Пункт
«`state-resource` — порядок деструкторов и линейное время» приходит с двумя
долгами C: обрыв в полёте, который дробление обязано снять, и порядок «ветка,
потом деструкторы», у которого свидетеля пока нет.

Ветка, зовущая `resume` не в хвосте: снятие
сегмента `adamas_kont_cut` в резумпцию-значение, возобновление ставит сегмент
обратно, дроп недожившей — `adamas_resumption_drop` с его договором «код после
деструкторов кладётся кадром до дропа». Здесь же `#handleState` и кадр
`Settling`: решение о смерти резумпции откладывается до конца применения
ответа к состоянию — дословно машина после вопроса 129. Дробление тел кадрами
(решение 3) — сердце трека и волны. Критерии: `state-resource` — порядок
деструкторов и **линейное время** (у машины мерено 88→420 мс на 3200→25600
операций; квадратичность понижения ловится тем же рядом); `Yield`/pipe-формы
корпуса; `eval/interpreter.adamas` как capstone — самый глубокий эффектный
свидетель корпуса.

*Как вышло.* Взято **62 из 83** против 48 из 81; `TAKEN` с 43 до 61. Корпус
пополнен двумя фикстурами (`eval/general-order`, `eval/general-frames`);
остальные двенадцать стояли за общей веткой либо за `#handleState`: `state`,
`state-resource`, `rows`, `resource`, `sequences`, `field-effect`,
`module-resource`, `operation-higher-order`, `instance-context-effect`,
`execution-positions`, `unwind-inner-handler`, `unwind-live-outer`. Свидетели
формы и времени — `crates/adamas-codegen/tests/general.rs`. Весь срез прогнан
под `-fsanitize=address,undefined` — чисто.

- **Ряд времени переписал построение рантайма, а не понижения.** Первый прогон
  дал 17/53/164/921 мс на 3200/6400/12800/25600 операций — квадрат, при том что
  хвостово-резумптивный сосед той же формы линеен (1/2/4/6). Различает их
  разрез: `adamas_kont_cut` обходил будущий сегмент ради длины, а сегмент общей
  ветки растёт с глубиной рекурсии. Довод прежней редакции — «копирование и
  раскрутка стоят столько же» — верен для мультишота и абортивной ветки и
  неверен для одношота: там возобновление есть перекладывание двух указателей.
  Длина снята с `adamas_kont` и `adamas_segment` и считается обходом там, где
  спрашивают. Стало **2/3/7/12 мс** — у машины на том же ряде 88/134/235/420.
- **Кадра `Settling` не понадобилось, и решение 4 в этой строке неверно.**
  Машине он был нужен потому, что она решает «резумпцию не позвали» на возврате
  ветки; у понижения на тот же вопрос отвечает **владение**: нить состояния
  держит резумпцию в замыкании `\s -> resume v s`, обрывающая ветка не держит
  нигде, и дроп ставит `perceus` обычным правилом. Ответ точнее машинного и
  точки решения не требует. `#handleState` понижается тем же путём, что
  одношот; различие остаётся только в имени, как и говорит вопрос 129.
- **Оба долга трека C сняты.** Обрыв в полёте ушёл целиком — `aborting`,
  `target`, `answer`, `adamas_kont_arm`/`disarm`, `adamas_kont_leave`,
  `adamas_kont_close` и **четвёртое место** в `tests/seam.rs`. Абортивная ветка
  теперь просто возвращает ответ: под срезом лежит кадр продолжения самого
  `handle`. Порядок «ветка, потом деструкторы» свидетеля получил — с живой
  резумпцией, как трек C и предсказал: `eval/general-order` даёт `[1, 7, 9, 0]`,
  а мутант, раскручивающий на месте дропа, роняет прогон.
- **Договор возврата у дроблёной функции другой**, и это то, что стоит знать
  прежде кода: она возвращает не свой ответ, а значение, которое трамплин
  обязан отдать вершине. Своё продолжение вызывающий кладёт кадром до вызова; в
  хвосте класть нечего. Отсюда же — трамплин снимает кадр `HANDLER` сам, а
  `adamas_frame_code` получил ручку стека: дроблёное тело есть цепочка таких
  кодов, и каждый вправе положить новые кадры.
- **Цена дробления названа числом.** Кадр — блок кучи, и ставится он на каждую
  точку приостановки с непустым продолжением: `handlers.rs` считает 12 блоков
  вместо 8, `abortive.rs` — плюс два на scope с ресурсом (`CLOSING` и `CLOSED`:
  нормальный выход идёт трамплином, а не прямым вызовом деструктора).
  Разностные утверждения тех же свидетелей не сдвинулись ни на одну.
- **Первая форма, применяя значение, заводит свой корень.** Граница замыкания
  динамическая, и за ней вправе оказаться дроблёная лямбда — с ресурсом либо со
  своим хендлером. Ячеек кучи корень не стоит: пустой стек — слово на кадре и
  пустой цикл. Прежний `NULL` на месте ручки был латентным дефектом, а не
  экономией.
- **Питомник получил собственный отказ у всех десяти файберных программ**
  (milestone 2 волны): за общей веткой у них стоял `withNursery`.
- **Capstone не взят, и держит его одна маска.** `eval/interpreter.adamas`
  падает на `#mask.Fail` — том самом отказе, который трек B оставил
  нерешённым («число пропусков вектору не считает никто»). Замер сделан
  заглушкой, понижающей `#mask.L e` как `e` без маски: capstone **собирается и
  считает**, расходясь с машиной ровно там, где маска и решает; `mask`
  расходится так же, `label-argument` обрывается. Значит за маской у capstone
  не стоит ничего — ни `#handleState`, ни общая ветка, ни ресурс, ни раскрутка.
  Заглушка откачена: она семантически неверна.

  *Развилка, которую трек не решает и возвращает владельцу.* Счётчиком
  пропусков маска не выражается вовсе, и это структурно, а не «пока нечем»:
  `skip` нужен **месту операции**, а операции лежат внутри маскируемого
  вычисления, то есть в чужой функции, и статического счёта у них нет.
  Единственная форма, которая ложится на evidence-перевод, — маска **снимает**
  ближайшую запись своей метки из вектора, под которым идёт вычисление; §3.4
  «маски считаются по одной» этим выполняется дословно (каждая снимает одну), а
  §10 вопрос 72 не трогается. Цена измерена: одна функция рантайма
  (`adamas_evidence_mask`), один узел IR и одна ветка эмиттера — около
  девяноста строк, новых понятий ноль. Против: шапка `adamas.h` говорит «маски
  считает вектор числом пропусков», и эту строку пришлось бы переписать.
  Аргумент `skip` у `adamas_evidence_lookup` при этом остаётся — его читает
  подавление.

  *Развилка закрыта владельцем 2026-09-12* — вариантом, который трек и нашёл;
  сделано в треке D′ ниже. Одна строка оценки не подтвердилась: `skip`
  подавление **не читает** (оно ставит флаг записи, а поиск отвечает
  трёхзначно), потребителя у аргумента не осталось ни одного, и он убран.

**D′. Маска — сделано 2026-09-12.** `#mask.L` понижается узлом `Expr::Mask`:
вектор без ближайшей записи своей метки (`adamas_evidence_mask`) и вычисление
под ним. Кадра маска не ставит вовсе — метка `MASKING` остаётся у рантайма для
симметрии с машиной, и только.

*Как вышло.* Взято **65 из 83** против 62; `TAKEN` с 61 до 65. Новых четыре:
capstone `eval/interpreter.adamas`, `eval/mask`, `eval/label-argument` и
`sequences` — последняя бралась уже треком D, но в список не попала. Capstone
сходится с машиной побайтово и кончает нулём живых блоков; он, `mask` и
`label-argument` прогнаны под `-fsanitize=address,undefined` — чисто.
Свидетели — `crates/adamas-codegen/tests/mask.rs`, шесть штук, плюс два в
`adamas-runtime/tests/evidence.rs`. Мутантов девять: восемь по коду, все
пойманы, и двое из восьми потребовали свидетеля, которого не было; девятый — по
самому свидетелю, и он **выжил**, чем и назвал цену симметричного пути.

- **Оценка трека D в девяносто строк промахнулась вдвое с лишним, и не там, где
  думалось.** `adamas_evidence_mask` — ровно 23 строки, как обещано; узел IR —
  23; две ветки эмиттера — 56; понижение — 89 (из них половина ушла на общий с
  хендлером запуск приостановленного вычисления). Сверх названных трёх мест
  узел прошёл ещё через **шесть** обходов — `children`, `children_mut`,
  `Anf::tail`, `Anf::stepped`, `perceus::effectful`, `plans`/`attach` — и через
  три копии `shape`; вышло около двухсот вставленных строк вместо девяноста.
  Цена узла IR — не его собственные строки, а число обходов, которые о нём
  обязаны знать.
- **Форм у маски в эмиттере две, а не одна.** Хвостовая — вычисление идёт
  хвостом куска, вектор уходит эпилогом; значение-форма — вычисление чисто, и
  вектор отдаётся на месте. Вторая казалась мёртвой и мёртвой не оказалась:
  `mask` над чистым вычислением пишется и понижается. Мутант, не отдающий там
  вектор, выживал, пока свидетеля не завели.
- **Capstone держала не маска.** Сняв её, прогон разошёлся с машиной **не** в
  позиции маски — та сошлась сразу, — а на отметке деструктора ресурса. Дефект
  оказался треков D: **резумпция в слоте замыкания не раскручивалась**. Нить
  состояния держит её в `\s -> resume v s`, и если вычисление оборвалось раньше
  применения, замыкание гибнет вместе с ней; `adamas_release_value` освобождал
  ручку сегмента обычным дропом, не выполнив ни одного деструктора внутри.
  `adamas_segment_abandon` — вторая из двух точек дропа, названных `adamas.h`, —
  до этой правки не звалась из порождённого кода ни разу. Свидетеля этому не
  давал ни один срез поодиночке: нужно, чтобы нить состояния, ресурс и обрыв
  встретились друг с другом, — а это ровно то, ради чего capstone и написан.
- **«Расходясь с машиной ровно там, где маска и решает» — сказано без замера.**
  Заглушка трека D показала расхождение, а не его место; мест оказалось два.
  Жанр знакомый: записанный диагноз проверяется пробной правкой, а не чтением.
- **Мутант «снимается последняя запись, метка не смотрится» пережил корпус
  целиком** и три свидетеля из четырёх: во всех них последняя запись вектора
  как раз своя, и различие сокращается. Ловит его хендлер **чужой** метки между
  маской и её хендлером — свидетель, заведённый мутантом.
- **Форма «ошибка сокращается на симметричном пути» измерена прямо.** Сделай оба
  одноимённых хендлера отвечающими одинаково — и мутант «маска не делает
  ничего» проходит свидетеля насквозь. Поэтому ответы в `mask.rs` различны, а
  сам ответ идёт списком; нульарный конструктор вместо числа — чтобы цена в
  блоках говорила про маску, а не про ответ.
- **Цена маски названа числом:** одна ячейка кучи на маску, и это вектор. Кадра
  нет — разница в две ячейки означала бы обратное, и свидетель это проверяет.
- **`adamas.h` переписан по факту в трёх местах**: раздел о векторе, «Чего здесь
  нет» и метка `MASKING`. Аргумент `skip` у `adamas_evidence_lookup` убран
  вместе с полем `skip` у `Expr::Perform` — потребителя не осталось ни одного.

*Остаток корпуса — 18, поимённо.* Питомник (Фаза 7): `await-value`, `cancel`,
`cancel-bystander`, `nursery`, `nursery-abort`, `spawn-local`, `task`,
`task-handoff`, `task-typed` — девять, а не десять: у `fibers` раньше питомника
встаёт `#handleMulti.Async`, и было это так уже при записи трека D. *(Поправка
трека E: питомника у `fibers` нет вовсе — она пишет планировщик сама, и
`withNursery` в ней не встречается.)* Мультишот
(трек E): `effects`, `fibers`, `multi-over-oneshot` — три. Не эффекты вовсе —
шесть: `function` (ответ
программы функция, печатать нечем), `prelude` и `sealed-effect` (операция
значением; у второй причина в счёте арности за синонимом), `flat-sealed-member`,
`region-strategy-handled`, `region-strategy-in-io` (боксирование и запечатанный
член, §4.11 и §5.1 — граница, названная треком B).

**Что досталось трекам E–F от D и D′.** Дроблёное тело, трамплин с `HANDLER` и
резумпция-значение готовы; мультишоту остаётся `adamas_segment_copy` (он уже
есть) плюс `#handleMulti.L` тем же путём, что `#handle.L`, с вердиктом
`General` у всех веток и без траты ручки при возобновлении. *(Поправка трека E:
«он уже есть» промахнулось. Записи вектора называют кадры, у копии они свои, и
копия обязана их переписать; см. раздел трека.)* Маска мультишоту не
добавляет ничего: вектор её живёт в кадрах, а копия сегмента дупает поля звена
вместе с ним. Разрез за
постоянное время держится на том, что длина не хранится: копия сегмента её
считает обходом, и это законно — копирование и так стоит длины. Треку F: ряд
времени гоняется `tests/general.rs`, полные четыре точки — руками, форма
программы там же.

**E. Multi-shot — сделано 2026-09-12.** `#handleMulti.L`: копирование звеньев
сегмента с `dup` полей — «segmented stack copy минус копирование стека».
Критерии: пять `handleMulti`-фикстур корпуса; свидетель двойного возобновления
с ресурсом — деструктор у каждой копии свой (машина — образец).

*Как вышло.* Взято **68 из 83** против 65; `TAKEN` с 65 до 68. Новых три, и это
весь мультишотный остаток: `effects`, `multi-over-oneshot` и `fibers`. Каждая
сверена с машиной, кончает нулём живых блоков и прогнана под
`-fsanitize=address,undefined` — чисто. Свидетели — `crates/adamas-codegen/tests/multi.rs`,
шесть штук, плюс три в `adamas-runtime/tests/frame.rs`. Мутантов восемь, семь
пойманы, один выжил с числом.

- **`fibers` держал не питомник.** Запись трека D («у `fibers` раньше питомника
  встаёт `#handleMulti.Async`») верна наполовину: `withNursery` эта программа не
  зовёт вовсе — планировщик файберов написан на самом языке, резумпция уезжает в
  поле конструктора, драйвер обычная рекурсия. Отказ у неё был один, и с ним она
  и уехала в `TAKEN`. Файберных программ в остатке девять, и все девять держит
  постулат.
- **Копии мало было звеньев, и это цена, которой в плане не было.** Запись
  вектора evidence называет **кадр** хендлера, а операция режет стек по этому
  указателю (`adamas_kont_cut`). У копии кадр свой, поэтому вторая операция под
  возобновлённым сегментом уходила резать по кадру оригинала, которого на стеке
  нет вовсе, — и сносила стек молча. `adamas_segment_copy` теперь даёт копии
  свои векторы и переписывает в них записи скопированных хендлеров
  (`adamas_evidence_rebind`). «Остаётся `adamas_segment_copy` (уже есть)» —
  оценка, промахнувшаяся на несущем месте.
- **Векторов у копии по одному на различный вектор, а не на звено.** Под одним
  хендлером цепочка звеньев несёт один и тот же указатель — вторая форма
  передаёт его по вызовам без изменений, — и копия это разделение сохраняет.
  Сними его — цена копии станет два блока на звено вместо одного; измерено
  мутантом, `+8` вместо `+4` на четырёх уровнях.
- **Кадр носит число счётных слотов.** Плоское значение лежит в слоте битами
  (§4.11), заголовка у него нет, и прежний `adamas_dup` по всем слотам писал бы
  счётчик по чужому адресу — `40` в слоте это чётный указатель. Счётные слоты
  идут первыми, `fields` и `counted` делят слово, среда осталась на 56-м байте.
  Довод тот же, каким `adamas.h` объясняет само число слотов: копирует и дропает
  кадры рантайм, а не порождённый по типу код.
- **Цена мультишота названа числом: одна ячейка кучи на звено сегмента.** Ряд —
  лестница из непустых продолжений, 2 и 6 уровней: одноходовая проба 9 и 13
  блоков, двуходовая 21 и 29. Второй ход стоит 12 и 16, то есть ровно `+1` на
  уровень. O(глубины) §3.4 этим и выполняется; в блоках копия есть звенья плюс
  вектор плюс ручка.
- **N возобновлений стоят N-1 копий.** Последняя ссылка на ручку копии не
  требует — возобновить второй раз некому, и сегмент отдаётся сам; договор об
  уникальности тот же, что у reuse (§5.1, §10 вопрос 149). Одноходовая
  мультишотная ветка поэтому не платит за копию вовсе. Мутант, копирующий
  всегда, ловится тем же рядом.
- **Свидетеля «двойное возобновление с ресурсом» не существует, и это законный
  исход.** §3.4 запрещает resource-типы в scope `handleMulti` статически, и
  проверка стоит в элаборации — до понижения такая программа не доходит вовсе
  (`a_resource_under_multishot_is_refused_before_any_copy`). Отказ приходит
  раньше копирования по построению, а не по удаче. Отказ `adamas_segment_copy`
  на кадре `UNWINDING` при этом свидетеля не имеет и иметь не может: кадр этот
  берётся от деструктора, а деструкторов под мультишотом не бывает.
- **Ручку в слоте копии копия помечает мультишотной.** Иначе первый ход тратил
  бы её, а второму доставалась пустая — тот самый дефект, который машина закрыла
  признаком `multishot`. С поверхности языка эту программу не построить: общая
  ветка одношотного хендлера под `handleMulti` отвергается кратностью (§3.4,
  `1`-связывание под ω-проходом), — поэтому свидетель стоит в рантайме, где
  договор и живёт. Ручка в слоте **замыкания** этим не покрыта: типов замыкания
  рантайм не знает, и второе возобновление там обрывает процесс названной
  ошибкой; граница записана в шапке `adamas.h`.
- **Мутант выжил один — форсированный вердикт.** «Вердикт `General` у всех
  веток» (решение трека) корпус не различает: вычисленный вердикт проходит и
  корпус, и все свидетели. Довод за форсированный — «resume в хвосте у
  ω-резумпции не значит „зовётся один раз“» — на письме верен, а программы за
  ним нет: `untail` требует единственного вхождения, и большего из него не
  выжать. Цена решения измерена: **один блок** на срабатывание хвостовой ветки
  мультишотного хендлера (9 против 8 на пробе глубины 2, 13 против 12 на
  глубине 6) — ручка сегмента, который тут же и тратится. Развилка оставлена
  владельцу: снять форсирование дешевле, чем удержать, но снятие меняет
  записанное решение волны.
- **Маска мультишоту действительно ничего не добавила** — единственная строка
  «что досталось», подтвердившаяся дословно.

*Остаток корпуса — 15, поимённо.* Питомник (Фаза 7): `await-value`, `cancel`,
`cancel-bystander`, `nursery`, `nursery-abort`, `spawn-local`, `task`,
`task-handoff`, `task-typed` — девять. Не эффекты вовсе — шесть: `function`
(ответ программы функция, печатать нечем), `prelude` и `sealed-effect` (операция
значением; у второй причина в счёте арности за синонимом), `flat-sealed-member`,
`region-strategy-handled`, `region-strategy-in-io` (боксирование и запечатанный
член, §4.11 и §5.1 — граница, названная треком B). Эффектного долга у волны не
осталось ни одной программы.

**Что досталось треку F от E.** Мультишот берётся, и корпус на нём зелёный;
бенчмарку остаётся то, ради чего он и заявлен, — чистый сосед против
tail-resumptive хендлера (milestone 3), и мультишот к нему отношения не имеет.
Точка `native/handled` по-прежнему упирается в боксирование плоского множителя
(трек B), и это единственное, что стоит между швом и числом. Мера корпуса —
68 из 83, остаток пятнадцатью именами выше.

**F. Бенчмарк и отчёт — сделано 2026-09-12.** `benches/` у понижения: чистый
сосед против tail-resumptive хендлера на одном алгоритме, число в §13
(milestone 3). Финальная мера корпуса: `TAKEN` числом, остаток — поимённо с
причиной (питомник; прочее — поимённо). Запись §13 о волне, обновление §9 и
этого документа.

*Как вышло.* Препятствие, названное треками B и E, **стоит по-прежнему**: точка
`native/handled` упирается в боксирование плоского множителя, и первым делом это
измерено, а не прочитано. Взят первый из двух законных выходов — точка мерится
сегодня, множитель не-плоский, и в шапке стенда названо, что мерится именно этот
вариант. Milestone 3 закрыт. Свидетели — `crates/adamas-codegen/benches/native.rs`;
мутантов два, оба пойманы.

- **Форм стало три, и третья — не симметрия, а условие честности числа.** Между
  `pure` и `handled` различий **два**: операция и представление множителя.
  Померив их вместе, стенд назвал бы ценой хендлера сумму. `Boxed` — тот же
  алгоритм с указательным множителем и **без** хендлера; разность
  `handled − boxed` есть операция и ничего сверх. Точка `pure` при этом не
  сдвинулась ни на строку исходника: строка §6 мерится ею.
- **Цена хвостово резумптивного хендлера названа числом: 1.4–1.5 раза и четыре
  ячейки кучи на операцию.** Три чистых прогона, медианы в миллисекундах
  (`floor` — 0.45):

  | глубина | rust | pure | boxed | handled | машина |
  |---|---|---|---|---|---|
  | 14 | 1.53 | 3.78 | 4.02 | 5.56 | 133 |
  | 16 | 5.27 | 12.7 | 13.3 | 18.8 | 539 |
  | 18 | двумодален | 45.9 | 46.6 | **69.5** | 2188 |

  Отношение `handled/boxed` за вычетом пола — 1.43 / 1.43 / 1.50. В блоках цена
  точная и не плавает: `handled − boxed` равно **ровно четырём блокам на
  операцию** на всех трёх глубинах (114701 − 49165 = 4·16384 и так далее). Это
  и есть точка отсчёта вопроса 74. Машину понижение под хендлером обгоняет в
  тридцать с лишним раз.
- **Обход боксирования обошёлся в один блок на лист, и это измерено, а не
  оценено.** Множитель — нульарный конструктор, объектом он не становится
  (ABI 2026-09-08), но применяет его к листу отдельная функция `scale`, и
  переиспользование ячейки листа через границу вызова не проходит: reuse
  внутрифункционален. `boxed` поэтому стоит `pure` плюс блок на лист, а по
  времени — плюс 1–6%. Платят его `boxed` и `handled` одинаково, поэтому в
  разность он не входит.
- **Дефект, найденный по дороге: понижение печатает C, который не
  собирается.** Если после операции в теле остаётся плоская арифметика
  (`Leaf (mulInt64 x (factor ask))`), куски дроблёного тела ходят через
  трамплин указателем, а несут `int64_t`: порождается `int64_t v6 = incoming;`
  и `return t3;` из функции с типом ответа `adamas_value`. Отвергнуть такую
  программу понижение **не отвергает** — граница §10 вопроса 158 здесь не
  проверяется, тогда как на ветке хендлера и на границе замыкания проверяется.
  Обойдено в стенде переносом арифметики в чистую `scale`; правка принадлежит
  не этому треку. *(Сделана треком G 2026-09-12, обход снят.)*
- **Решение 3 волны в одной строке точнее, чем записано.** Кадр ставится не на
  всякий вызов второй формы, а только там, откуда приостановка достижима.
  Показал это мутант: у него `scaled` осталась второй формой — берёт `ev` и
  `kont`, — а `adamas_kont_push` в порождённом C не встречается ни разу, и
  рекурсия идёт прямыми вызовами. Цена второй формы поэтому платится не за
  форму, а за достижимую операцию, и вопрос 74 бьёт ровно туда.
- **Мутанта два, оба пойманы.** Работа: `scaled (Node l r) = Node l r` — ответ
  меняется (309237252096 → 103079084032, ровно треть), время на глубине 16
  падает 18.8 → 9.9 мс, блоки 1835025 → 524308; заодно сработал `assert_eq!` у
  соседа на Rust, раньше всех прочих. Операция: `scale ask x` → `scale Triple x`
  при сохранённых row, эффекте и хендлере — ответ тот же, а разность точек
  исчезает целиком (4.03 против 4.06 мс, 49168 против 49165 блоков).
- **Сосед на Rust двумодален, и первая половина milestone 3 этим
  переоткрывается.** Семь прогонов: `rust/18` ложится в 21–24 мс либо в
  57–64 мс, мода видна уже на `rust/16` и устанавливается до замера, внутри
  прогона интервал узок в обеих модах. Точки `native/*` стабильны во всех семи.
  Значит вывод 2026-09-11 — «на глубине 18 понижение обгоняет Rust в полтора
  раза» — прочитан с медленной моды: против быстрой понижение медленнее вдвое.
  Строка §6 «паритет с Rust или лучше» не берётся и не опровергается; она
  требует прогонов, воспроизводимых между запусками, а таких у стенда пока нет.
  Жанр знакомый и записан прежде: число, снятое одним прогоном, есть заявка на
  ряд прогонов.
- **Мера корпуса печатается прогоном.** `TAKEN` сверялся руками трижды за
  волну; теперь `tests/agreement.rs` под `--nocapture` печатает «взято N из M»
  и каждый отказ с причиной, и переписывать список неоткуда.

**G. Граница куска дроблёного тела — сделано 2026-09-12, после волны.** Дефект,
найденный треком F и записанный §10 вопросом 164: понижение отвечает `Ok` на
программу, чей C не собирается. Сорт дефекта хуже размера — это **не** отказ
понижения, а молчаливое расхождение трёх вычислителей там, где договор обязан
их держать.

*Как вышло.* Взят вариант **(б)**: граница переводит. Взято **69 из 84**
против 68 из 83; корпус пополнен одной фикстурой
(`eval/flat-across-a-suspension`). Свидетели — `crates/adamas-codegen/tests/boundary.rs`,
четыре штуки. Мутантов пять: четыре по коду, все пойманы, пятый по свидетелю, и
он **выжил**, чем и назвал цену симметричного пути.

- **Половин у границы три, мера была написана у одной.** Слот среды кадра уже
  переводил плоское значение битами (`adamas_slot_of`/`adamas_slot_word`,
  `flat.c`); пришедшее в кусок и ответ куска трамплину отдавали его как есть.
  Правка — два общих помощника и представление, протянутое в `finish`; тремя
  местами она и кончилась. Постановка вопроса 164 в этом месте точна: «недостаёт
  одной позиции» — недоставало двух, и обе половины одной и той же границы.
- **Вариант (а) дешевле и делает не то, и это измерено пробной правкой.**
  Отказ на границе куска — 32 добавленных строки против 51 у (б); корпус тот же
  (68 из 83, регрессий нет), отказ назван: «`measured`: int8_t уходит из куска
  дроблёного тела — граница носит указатель». Но свидетель 164 остаётся
  отвергнутым, корпус не растёт, а точка §6 «эффект над плоскими данными» не
  мерится по-прежнему. Пятнадцать строк экономии против заказчика правки —
  выбор (б).
- **Перевод не стоит ни одной ячейки кучи, и это число, а не довод.** Стенд:
  `handled-flat − handled` равно **ровно двум блокам на лист** на всех трёх
  глубинах (147469 − 114701 = 2·16384 и так далее) — то есть двум лишним кадрам
  дробления, которые даёт третья точка приостановки против первой. Боксирования
  в этой разности нет.
- **Обход в стенде снят, точка над плоским множителем измерена.** Четвёртая
  форма `handled-flat`: `Leaf (mulInt64 x (factor ask))`. Медианы (мс, `floor`
  0.455): pure 3.83 / 13.13 / 47.12, boxed 4.09 / 14.40 / 48.25, handled
  5.66 / 18.99 / 67.62, **handled-flat 6.12 / 20.27 / 76.09**. За вычетом пола
  `handled-flat / pure` — **1.68 / 1.56 / 1.62**, и это и есть цена эффекта над
  плоскими данными. В блоках она точна: семь на лист — четыре на операцию, два
  на лишние кадры, один на переиспользование ячейки, не проходящее через кусок
  (reuse внутрифункционален, а тут он ещё и внутрикусочен).
- **Ошибка сокращается на симметричном пути, и мерено это прямо.** Мутант
  «перевод всегда шириной `Int64`» на целых **ненаблюдаем вовсе** — C приводит
  число обратно, — и ловит его только `Float64` с дробной частью. Пятый мутант
  это подтвердил: замени в свидетеле 2.5 на 3.0, и мутант проходит корпус
  целиком (69 из 84, зелено); остаётся один текстовый свидетель, смотрящий на
  представление. Отсюда `Float64` в фикстуре и требование дробной части,
  записанное в её шапке.
- **Соседние позиции остались отказом, и каждая названа.** Ответ второй формы —
  «операция в функции с плоским ответом» (`EmitError::Aborting`, был и до
  трека); вычисление под хендлером и тело замыкания — «ожидалось указательное
  значение, а пришло плоское» (`LowerError::Representation`). Кадр `CLOSING`
  плоского значения не получает ни одним написуемым путём: всякий маршрут к
  нему проходит через одну из названных позиций — проверено пробами, не
  чтением.

## Чем кончилась волна

Записано последним треком, 2026-09-12, по образцу раздела «Чем фаза кончилась»
в `docs/phase6-plan.md`.

**Milestone волны взят целиком, все три меры.** (1) Эффектный хвост без
питомника переехал в `TAKEN`: корпус **68 из 83** против 37 из 79 на старте,
`TAKEN` с 32 до 68. Заказчик первой половины пункта — пользовательская
стратегия, различимая числами наравне со встроенными, — выполнен `tests/strategy.rs`,
а не фикстурой, которую пункт называл: та держится не на эффектах. (2) Питомник
отвергается собственным отказом у всех девяти файберных программ. (3) Первый
бенчмарк понижения есть, и он дал вопросу 74 число: 1.4–1.5 раза и четыре
ячейки кучи на операцию.

**Milestone Фазы 6 при этом по-прежнему не взят, и причина сменилась целиком.**
Прежняя — «вторая форма понижения отложена» — снята: вторая форма написана от
селекции форм до мультишота, эффектного долга у корпуса не осталось. Новых две.
Программы нужного размера нет: самая большая взятая — capstone на **473 строки**,
до пятисот недостаёт двадцати семи, и писать её никому не поручалось. И native
performance **не установлена**: замер есть, а строка §6 им не берётся, потому
что сосед на Rust двумодален между прогонами. Запись §9 приведена к этому факту.

**Остаток корпуса — 15, поимённо, прогоном 2026-09-12.** *(Знаменатель после
трека G — 84: корпус пополнен `flat-across-a-suspension`, и она берётся;
остаток тот же список из пятнадцати.)* Питомник, Фаза 7 —
девять: `await-value`, `cancel`, `cancel-bystander`, `nursery`, `nursery-abort`,
`spawn-local`, `task`, `task-handoff`, `task-typed`; у всех девяти отказ один и
тот же — «`withNursery` — питомник (§5.2): таблицы файберов в рантайме нет».
Не эффекты вовсе — шесть: `function` (ответ программы функция, печатать нечем);
`prelude` и `sealed-effect` (операция значением; у второй причина в счёте арности
за синонимом, §10 вопрос 153); `flat-sealed-member` (ответ функции — дескриптор
укладки, а пришла запись); `region-strategy-handled` и `region-strategy-in-io`
(плоское значение через границу вызова, §4.11 и §5.1).

**Что достаётся Фазе 7 — сверх записанного итогом фазы.**

- ~~**Боксирование через границу замыкания (§10 вопрос 158) подорожало в
  постановке.**~~ **Половина снята треком G** (2026-09-12): граница куска
  дроблёного тела переводит плоское значение в слово, свидетель 164
  собирается и сходится с машиной, точка §6 «эффект над плоскими данными»
  измерена — 1.56–1.68 раза против чистого соседа. Ниже — как было записано.
  Оно держит не только `region-strategy-*`, но и всякую пробу
  эффекта над плоским значением — включая ту, которой мерится §6. И у него
  нашлась **непроверяемая половина**: плоская арифметика после операции внутри
  дроблёного тела не отвергается, а печатается несобирающимся C. Ветка хендлера
  и граница замыкания проверяются, граница куска тела — нет.
  **Что осталось Фазе 7:** граница **вызова** и **замыкания** —
  `region-strategy-handled` и `region-strategy-in-io`, два последних плоских
  отказа корпуса. Мера у них другая: слот замыкания живёт дольше кадра, и
  словом там не обойтись.
- **Вопрос 74 (инлайнинг хвостово-резумптивных хендлеров) получил число, от
  которого считать выигрыш,** и заодно уточнение адреса: платит не «функция
  второй формы», а функция, из которой достижима операция. Верхняя граница
  выигрыша — те самые 1.4–1.5 раза и четыре блока на операцию.
- **У §6 нет методики, воспроизводимой между прогонами.** Двумодальность соседа
  на Rust — не шум замера, а свойство прогона: она устанавливается до первой
  выборки и держится весь процесс. Пока это не снято, любая строка §6,
  сравнивающая с соседом в том же процессе, читается модой, а не кодом. Это
  предпосылка сквозного трека Фазы 7 «измерение разрыва C против LLVM»: он
  меряет ту же величину тем же способом.
- **Программа на 500 строк** — ни в корпусе, ни в `docs/examples/`. Milestone
  Фазы 6 без неё не закрывается арифметически, сколько бы корпус ни брал.

**Что волна оставляет в хорошем состоянии.** Вторая форма понижения целиком:
селекция по row, кадры, вердикт ветки, дробление тел, маска, мультишот — всё
сверено с машиной, всё чисто под санитайзерами. Мера корпуса печатается
прогоном, а не переписывается. Стенд §6 несёт свою защиту в шапке: `floor`,
сверка ответа с машиной, три точки по глубине, два мутанта у новой точки и
названная двумодальность соседа. *(Трек G добавил четвёртую форму
`handled-flat` и снял обход, которым стенд жил.)*

**Метод, окупившийся и в этой волне.** Записанный диагноз промахивается: «шов
готов, появится боксирование — точка замерится без правки файла» оказалось
верным наполовину (боксирование по-прежнему нужно, но обойти его удалось), а
«между `pure` и `handled` одно различие» — неверным вовсе, и стоило это третьей
формы. Цена варианта, названная словами, есть заявка на прогон — за волну это
подтвердилось у `adamas_segment_copy` («уже есть»), у оценки маски в девяносто
строк и здесь. *Трек G добавил четвёртое подтверждение и первое обратного
знака: цена варианта (а) угадана верно — он и вправду вышел вдвое короче, — но
вывод из неё («стартовая гипотеза — (а)») промахнулся, потому что размер правки
не был тем, чем стоило выбирать. Мерить надо было заказчика, а не строки.*

## Границы, названные заранее

- **Питомник и файберы** — Фаза 7, вместе с таблицей файберов рантайма;
  волна лишь даёт им собственный отказ (milestone 2).
- **Инлайнинг tail-resumptive хендлеров (вопрос 74)** — после волны: это
  оптимизация поверх работающей второй формы, у неё два заказчика
  (перф-цели §6, пользовательские стратегии без цены кадра) и число из
  milestone 3, от которого считать выигрыш.
- **`@noalloc` и хвостовая резумптивность** — уточнение вердикта («всякая
  операция аллоцирует» → по форме ветки) идёт следом за треком B, когда
  вердикт ветки материализован; в волну не входит, названо здесь, чтобы не
  потерялось.
- **Специализация по row** (вопрос 74, вариант б) — не трогается: волна
  living с evidence-косвенностью, и это записанная цена.

## Развилка, оставленная владельцу треком E — решена 2026-09-12

**Оставлено форсированным.** Довод: цена измерена и мала (один блок на
срабатывание), а платит её только `handleMulti` — по §3.4 «не бесплатный
default, а осознанный опт-ин», то есть ровно та форма, которой цена и
адресована. Безопасность вычисленного вердикта держится на том, что `untail`
требует единственного вхождения `resume`, — это свойство **реализации**, а не
записанное правило языка; менять запись волны на такой опоре дороже одного
блока. Ниже — развилка, как её вернул трек.

**Форсированный вердикт мультишотной ветки свидетеля не имеет.** Решение волны
говорит «`General` у всех веток `handleMulti`»; вычисленный вердикт проходит и
корпус целиком, и все свидетели мультишота. Довод за форсирование — «`resume` в
хвосте у ω-резумпции не значит „зовётся один раз“» — верен на письме и не
предъявляет программы: `untail` снимает хвост только при **единственном**
вхождении, а из одного вхождения второго вызова не получить. Цена форсирования
измерена — один блок кучи на срабатывание хвостовой ветки мультишотного
хендлера. Против снятия: решение записано волной, и снимать его на измерении
одной формы — работа владельца, а не трека.

## Порядок при одной сессии

A → B → C → D → D′ → E → F → G. B — первый наблюдаемый снаружи шаг (стратегии в C), и
если сессия одна, останавливаться после него уже осмысленно: пользовательские
стратегии — заявленный заказчик волны. D — самый тяжёлый и не делится; в него
не входить без зелёного C.

## Правила

Те же три, что купила фаза, плюс одно её собственное.

- **Критерий готовности — программа.** Пара фикстур либо прогон со сверкой
  `agreed`; отказ называет ближайшего проходящего соседа.
- **Мутант обязателен.** Отдельно помнить форму «ошибка сокращается на
  симметричном пути»: у кадров она ожидаема на порядке звеньев и на
  копировании сегмента — свидетель обязан видеть порядок числом или текстом.
- **Цена варианта, названная словами, — заявка на прогон.**
- **Повторить машину, а не изобрести.** Расхождение с интерпретатором — дефект
  понижения по определению; `agreement` — судья, capstone — глубина.
