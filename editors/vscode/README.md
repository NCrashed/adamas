# Adamas for VS Code

Diagnostics from the Adamas language server (`adamas-lsp`): the same errors
`adamas check` prints, inline and on the exact position.

Requires `adamas-lsp` on `PATH`, or set `adamas.server.path`.

Syntax highlighting is not here yet — see `editors/README.md` in the repository.
